%strong New comment
= form_for [@user,@car, @comment] do |f|
  -#= f.error_messages
  %p
    = f.label :text
    %br/
    = f.text_area :text
    =f.hidden_field :user_id, :value => @user.id
  = f.submit

